name: Deploy Supabase and GitHub Pages

on:
  workflow_run:
    workflows:
      - CI
    types:
      - completed
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: production-supabase-pages
  cancel-in-progress: false

jobs:
  migrate:
    name: Apply Supabase migrations
    if: >-
      (github.event_name == 'workflow_dispatch' &&
      github.ref == 'refs/heads/main') ||
      (github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.event == 'push' &&
      github.event.workflow_run.head_branch == 'main' &&
      github.event.workflow_run.head_repository.full_name == github.repository)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment: production
    env:
      SUPABASE_PROJECT_REF: ${{ vars.SUPABASE_PROJECT_REF }}
      SUPABASE_PUBLISHABLE_KEY: ${{ vars.SUPABASE_PUBLISHABLE_KEY }}
      EXPECTED_SUPABASE_PROJECT_REF: iyckwryrucqrxwlowxow
      SUPABASE_TELEMETRY_DISABLED: "1"
    steps:
      - name: Check out repository
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.0.0
        with:
          python-version: "3.12"

      - name: Set up Supabase CLI
        uses: supabase/setup-cli@46f7f98c7f948ad727d22c1e67fab04c223a0520 # v3.0.0
        with:
          version: 2.109.1
          github-token: ${{ github.token }}

      - name: Require production Supabase coordinates
        shell: bash
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_OWNER_EMAIL: ${{ secrets.SUPABASE_OWNER_EMAIL }}
          RUNWAYML_API_SECRET: ${{ secrets.RUNWAYML_API_SECRET }}
          FAL_KEY: ${{ secrets.FAL_KEY }}
          HEYGEN_API_KEY: ${{ secrets.HEYGEN_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          YOUTUBE_DATA_API_KEY: ${{ secrets.YOUTUBE_DATA_API_KEY }}
          CONTENTENGINE_WORKER_SECRET: ${{ secrets.CONTENTENGINE_WORKER_SECRET }}
        run: |
          if [ -n "${RUNWAYML_API_SECRET:-}" ]; then
            echo "::add-mask::$RUNWAYML_API_SECRET"
          fi
          if [ -n "${FAL_KEY:-}" ]; then
            echo "::add-mask::$FAL_KEY"
          fi
          if [ -n "${HEYGEN_API_KEY:-}" ]; then
            echo "::add-mask::$HEYGEN_API_KEY"
          fi
          if [ -n "${OPENAI_API_KEY:-}" ]; then
            echo "::add-mask::$OPENAI_API_KEY"
          fi
          if [ -n "${YOUTUBE_DATA_API_KEY:-}" ]; then
            echo "::add-mask::$YOUTUBE_DATA_API_KEY"
          fi
          if [ -n "${CONTENTENGINE_WORKER_SECRET:-}" ]; then
            echo "::add-mask::$CONTENTENGINE_WORKER_SECRET"
          fi
          : "${SUPABASE_ACCESS_TOKEN:?Configure SUPABASE_ACCESS_TOKEN in the production environment}"
          : "${SUPABASE_OWNER_EMAIL:?Configure SUPABASE_OWNER_EMAIL in the production environment}"
          : "${RUNWAYML_API_SECRET:?Configure RUNWAYML_API_SECRET in the production environment}"
          : "${FAL_KEY:?Configure FAL_KEY in the production environment}"
          : "${OPENAI_API_KEY:?Configure OPENAI_API_KEY in the production environment}"
          : "${CONTENTENGINE_WORKER_SECRET:?Configure CONTENTENGINE_WORKER_SECRET in the production environment}"
          : "${SUPABASE_PROJECT_REF:?Configure SUPABASE_PROJECT_REF as a repository variable}"
          : "${SUPABASE_PUBLISHABLE_KEY:?Configure SUPABASE_PUBLISHABLE_KEY as a repository variable}"
          if [ "${#CONTENTENGINE_WORKER_SECRET}" -lt 32 ]; then
            echo "CONTENTENGINE_WORKER_SECRET must contain at least 32 characters" >&2
            exit 1
          fi
          if [ "$SUPABASE_PROJECT_REF" != "$EXPECTED_SUPABASE_PROJECT_REF" ]; then
            echo "SUPABASE_PROJECT_REF does not match the reviewed production project" >&2
            exit 1
          fi

      - name: Apply immutable migrations and private grading keys
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_EXAM_KEYS_B64: ${{ secrets.SUPABASE_EXAM_KEYS_B64 }}
          SUPABASE_TRAINING_KEYS_B64: ${{ secrets.SUPABASE_TRAINING_KEYS_B64 }}
        run: >-
          python scripts/deploy_supabase_management_api.py
          --migrations-dir supabase/migrations

      - name: Apply versioned Auth and API configuration
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: supabase config push --project-ref "$SUPABASE_PROJECT_REF"

      - name: Synchronize private Runway API secret
        shell: bash
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          RUNWAYML_API_SECRET: ${{ secrets.RUNWAYML_API_SECRET }}
        run: |
          echo "::add-mask::$RUNWAYML_API_SECRET"
          supabase secrets set \
            --project-ref "$SUPABASE_PROJECT_REF" \
            RUNWAYML_API_SECRET="$RUNWAYML_API_SECRET"

      - name: Synchronize private fal.ai API secret
        shell: bash
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          FAL_KEY: ${{ secrets.FAL_KEY }}
        run: |
          echo "::add-mask::$FAL_KEY"
          supabase secrets set \
            --project-ref "$SUPABASE_PROJECT_REF" \
            FAL_KEY="$FAL_KEY"

      - name: Synchronize private HeyGen API secret
        shell: bash
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          HEYGEN_API_KEY: ${{ secrets.HEYGEN_API_KEY }}
        run: |
          # Ключ необязателен: маршрут «Дуэта» выключен, и без ключа он просто
          # не исполнится — отказ придёт до резервирования денег. Пропускаем
          # тихо, а не роняем развёртывание тем, у кого HeyGen ещё нет.
          if [ -z "${HEYGEN_API_KEY:-}" ]; then
            echo "HEYGEN_API_KEY is not configured; skipping Duet presenter secret"
            exit 0
          fi
          echo "::add-mask::$HEYGEN_API_KEY"
          supabase secrets set \
            --project-ref "$SUPABASE_PROJECT_REF" \
            HEYGEN_API_KEY="$HEYGEN_API_KEY"

      - name: Synchronize private OpenAI API secret
        shell: bash
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          echo "::add-mask::$OPENAI_API_KEY"
          supabase secrets set \
            --project-ref "$SUPABASE_PROJECT_REF" \
            OPENAI_API_KEY="$OPENAI_API_KEY"

      - name: Synchronize optional YouTube Data API secret
        shell: bash
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          YOUTUBE_DATA_API_KEY: ${{ secrets.YOUTUBE_DATA_API_KEY }}
        run: |
          if [ -z "${YOUTUBE_DATA_API_KEY:-}" ]; then
            if supabase secrets list \
              --project-ref "$SUPABASE_PROJECT_REF" \
              --output json \
              | python -c 'import json, sys; values = json.load(sys.stdin); raise SystemExit(0 if any(isinstance(value, dict) and value.get("name") == "YOUTUBE_DATA_API_KEY" for value in values) else 1)'
            then
              echo "YOUTUBE_DATA_API_KEY is not configured; removing the previously synchronized ingestion key."
              supabase secrets unset YOUTUBE_DATA_API_KEY \
                --project-ref "$SUPABASE_PROJECT_REF"
            else
              echo "YOUTUBE_DATA_API_KEY is not configured and no synchronized ingestion key exists."
            fi
            exit 0
          fi
          echo "::add-mask::$YOUTUBE_DATA_API_KEY"
          if [[ ! "$YOUTUBE_DATA_API_KEY" =~ ^[A-Za-z0-9_-]{20,256}$ ]]; then
            echo "YOUTUBE_DATA_API_KEY must contain 20 to 256 URL-safe characters" >&2
            exit 1
          fi
          supabase secrets set \
            --project-ref "$SUPABASE_PROJECT_REF" \
            YOUTUBE_DATA_API_KEY="$YOUTUBE_DATA_API_KEY"

      - name: Synchronize private background worker secret
        shell: bash
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          CONTENTENGINE_WORKER_SECRET: ${{ secrets.CONTENTENGINE_WORKER_SECRET }}
        run: |
          echo "::add-mask::$CONTENTENGINE_WORKER_SECRET"
          supabase secrets set \
            --project-ref "$SUPABASE_PROJECT_REF" \
            CONTENTENGINE_WORKER_SECRET="$CONTENTENGINE_WORKER_SECRET"

      - name: Synchronize optional signed email webhook secret
        shell: bash
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          RESEND_WEBHOOK_SECRET: ${{ secrets.RESEND_WEBHOOK_SECRET }}
        run: |
          if [ -z "${RESEND_WEBHOOK_SECRET:-}" ]; then
            if supabase secrets list \
              --project-ref "$SUPABASE_PROJECT_REF" \
              --output json \
              | python -c 'import json, sys; values = json.load(sys.stdin); raise SystemExit(0 if any(isinstance(value, dict) and value.get("name") == "RESEND_WEBHOOK_SECRET" for value in values) else 1)'
            then
              echo "RESEND_WEBHOOK_SECRET is not configured; removing the previously synchronized webhook secret."
              supabase secrets unset RESEND_WEBHOOK_SECRET \
                --project-ref "$SUPABASE_PROJECT_REF"
            else
              echo "RESEND_WEBHOOK_SECRET is not configured and no synchronized webhook secret exists."
            fi
            exit 0
          fi
          echo "::add-mask::$RESEND_WEBHOOK_SECRET"
          python scripts/validate_resend_webhook_secret.py
          supabase secrets set \
            --project-ref "$SUPABASE_PROJECT_REF" \
            RESEND_WEBHOOK_SECRET="$RESEND_WEBHOOK_SECRET"

      - name: Require reviewed authentication modes for creator Edge Functions
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import tomllib

          config = tomllib.loads(
              Path("supabase/config.toml").read_text(encoding="utf-8")
          )
          for function_name in (
              "creator-invite",
              "creator-set-password",
              "creator-generate",
              "creator-product-research",
              "creator-research-ingestion",
              "creator-ai-case-import",
              "creator-content-review",
              "creator-access",
          ):
              entrypoint = Path("supabase/functions") / function_name / "index.ts"
              if not entrypoint.is_file():
                  raise SystemExit(
                      f"Missing {function_name} Edge Function entrypoint"
                  )
              function_config = config.get("functions", {}).get(
                  function_name, {}
              )
              if function_config.get("verify_jwt") is not True:
                  raise SystemExit(
                      f"{function_name} must set verify_jwt = true"
                  )
          for function_name in (
              "creator-background-worker",
               "creator-recovery",
              "auth-email-webhook",
          ):
              entrypoint = (
                  Path("supabase/functions") / function_name / "index.ts"
              )
              if not entrypoint.is_file():
                  raise SystemExit(
                      f"Missing {function_name} Edge Function entrypoint"
                  )
              function_config = config.get("functions", {}).get(
                  function_name, {}
              )
              if function_config.get("verify_jwt") is not False:
                  raise SystemExit(
                      f"{function_name} must set verify_jwt = false"
                  )
          PY

      - name: Deploy authenticated creator invitation function
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: supabase functions deploy creator-invite --project-ref "$SUPABASE_PROJECT_REF"

      - name: Deploy authenticated required password change function
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: supabase functions deploy creator-set-password --project-ref "$SUPABASE_PROJECT_REF"

      - name: Deploy authenticated real generation function
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: supabase functions deploy creator-generate --project-ref "$SUPABASE_PROJECT_REF"

      - name: Deploy authenticated product research function
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: supabase functions deploy creator-product-research --project-ref "$SUPABASE_PROJECT_REF"

      - name: Deploy authenticated research ingestion function
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: supabase functions deploy creator-research-ingestion --project-ref "$SUPABASE_PROJECT_REF"

      - name: Deploy authenticated AI historical case import function
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: supabase functions deploy creator-ai-case-import --project-ref "$SUPABASE_PROJECT_REF"

      - name: Deploy authenticated content review function
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: supabase functions deploy creator-content-review --project-ref "$SUPABASE_PROJECT_REF"

      - name: Deploy secret-authenticated background worker function
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: >-
          supabase functions deploy creator-background-worker
          --project-ref "$SUPABASE_PROJECT_REF"
          --no-verify-jwt

      - name: Configure native Supabase background worker cron
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          CONTENTENGINE_WORKER_SECRET: ${{ secrets.CONTENTENGINE_WORKER_SECRET }}
        run: python scripts/configure_supabase_background_cron.py

      - name: Deploy authenticated account access function
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: >-
          supabase functions deploy creator-access
          --project-ref "$SUPABASE_PROJECT_REF"

      - name: Deploy public idempotent password recovery function
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: >-
          supabase functions deploy creator-recovery
          --project-ref "$SUPABASE_PROJECT_REF"
          --no-verify-jwt

      - name: Deploy public privacy-minimized click redirect
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: >-
          supabase functions deploy creator-click
          --project-ref "$SUPABASE_PROJECT_REF"
          --no-verify-jwt

      - name: Deploy signed email delivery webhook
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: >-
          supabase functions deploy auth-email-webhook
          --project-ref "$SUPABASE_PROJECT_REF"
          --no-verify-jwt

  build-pages:
    name: Build browser-only creator workspace
    if: >-
      (github.event_name == 'workflow_dispatch' &&
      github.ref == 'refs/heads/main') ||
      (github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.event == 'push' &&
      github.event.workflow_run.head_branch == 'main' &&
      github.event.workflow_run.head_repository.full_name == github.repository)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      pages: read
    env:
      SUPABASE_PROJECT_REF: ${{ vars.SUPABASE_PROJECT_REF }}
      SUPABASE_PUBLISHABLE_KEY: ${{ vars.SUPABASE_PUBLISHABLE_KEY }}
      EXPECTED_SUPABASE_PROJECT_REF: iyckwryrucqrxwlowxow
    steps:
      - name: Check out repository
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}
          persist-credentials: false

      - name: Configure GitHub Pages
        uses: actions/configure-pages@983d7736d9b0ae728b81ab479565c72886d7745b # v5.0.0

      - name: Assemble static site with browser-safe configuration
        shell: bash
        run: |
          : "${SUPABASE_PROJECT_REF:?Configure SUPABASE_PROJECT_REF as a repository variable}"
          : "${SUPABASE_PUBLISHABLE_KEY:?Configure SUPABASE_PUBLISHABLE_KEY as a repository variable}"
          if [ "$SUPABASE_PROJECT_REF" != "$EXPECTED_SUPABASE_PROJECT_REF" ]; then
            echo "SUPABASE_PROJECT_REF does not match the reviewed production project" >&2
            exit 1
          fi
          case "$SUPABASE_PUBLISHABLE_KEY" in
            sb_publishable_*) ;;
            *) echo "SUPABASE_PUBLISHABLE_KEY must be a browser-safe sb_publishable_ key" >&2; exit 1 ;;
          esac
          python scripts/build_pages_release.py \
            --source-dir web/app \
            --output-dir _site \
            --edge-function supabase/functions/creator-generate/index.ts \
            --project-ref "$SUPABASE_PROJECT_REF" \
            --expected-project-ref "$EXPECTED_SUPABASE_PROJECT_REF" \
            --publishable-key "$SUPABASE_PUBLISHABLE_KEY"

      - name: Reject server secrets and localhost from Pages artifact
        shell: bash
        run: |
          if grep -n -E 'sb_secret_|service_role|SUPABASE_ACCESS_TOKEN|RUNWAYML_API_SECRET|FAL_KEY|OPENAI_API_KEY|YOUTUBE_DATA_API_KEY' _site/config.js; then
            echo "Server-only material entered the generated browser configuration" >&2
            exit 1
          fi
          if grep -R -n -E 'sb_secret_[A-Za-z0-9._-]{16,}|sbp_[A-Za-z0-9._-]{16,}|postgres(ql)?://[^[:space:]"]+@' _site; then
            echo "A value shaped like a server credential entered the Pages artifact" >&2
            exit 1
          fi
          if grep -R -n -E '__SET_SUPABASE_|127\.0\.0\.1|localhost' _site; then
            echo "A placeholder or local endpoint entered the Pages artifact" >&2
            exit 1
          fi

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@7b1f4a764d45c48632c6b24a0339c27f5614fb0b # v4.0.0
        with:
          path: _site

  deploy-pages:
    name: Publish creator workspace
    needs:
      - migrate
      - build-pages
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e # v4.0.5

  bootstrap-owner:
    name: Provision first owner after Pages is live
    needs:
      - deploy-pages
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment: production
    permissions:
      contents: read
    env:
      SUPABASE_PROJECT_REF: ${{ vars.SUPABASE_PROJECT_REF }}
      SUPABASE_PUBLISHABLE_KEY: ${{ vars.SUPABASE_PUBLISHABLE_KEY }}
      EXPECTED_SUPABASE_PROJECT_REF: iyckwryrucqrxwlowxow
    steps:
      - name: Check out deployed repository revision
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.0.0
        with:
          python-version: "3.12"

      - name: Provision first owner and send password setup
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_OWNER_EMAIL: ${{ secrets.SUPABASE_OWNER_EMAIL }}
        run: python scripts/bootstrap_supabase_owner.py --display-name "Сергей"
